#pragma once

namespace ${PROJECT_NAMESPACE} {

constexpr static auto ${PROJECT_NAME_DEFINE}_VERSION_MAJOR = @VERSION_MAJOR@;
constexpr static auto ${PROJECT_NAME_DEFINE}_VERSION_MINOR = @VERSION_MINOR@;
constexpr static auto ${PROJECT_NAME_DEFINE}_VERSION_PATCH = @VERSION_PATCH@;
constexpr static auto ${PROJECT_NAME_DEFINE}_VERSION_BUILD = @VERSION_BUILD@;

constexpr static char[] ${PROJECT_NAME_DEFINE}_VERSION = "@VERSION@";

}

